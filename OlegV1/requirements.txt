# Python >= 3.9 required (for tuple[...] type hints)
# Tested on Python 3.12

# Core geospatial
geopandas>=1.0.0,<2.0.0
osmnx>=2.0.0,<3.0.0          # features_from_polygon / features_from_place API (>=1.9)
shapely>=2.0.0,<3.0.0

# WFS / HTTP
requests>=2.28.0
fiona>=1.9.0                  # backend for gpd.read_file() with WFS URLs

# Utilities
numpy>=1.24.0
pandas>=2.0.0
